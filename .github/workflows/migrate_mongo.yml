name: Migrate MongoDb

on:
  push:
    branches: [main, staging]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install migrate-mongo
        run: npm install -g migrate-mongo
      - name: Migrate
        run: cd .. && migrate-mongo up
        env:
          MONGO_URL: '${{ secrets.MONGO_URL }}'